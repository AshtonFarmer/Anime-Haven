# Release 62 migration reference

These encoded package parts and the original monolithic Arsenal source are retained only for KageNexus 2.0 comparison and extraction checks. Nothing in the active app, service worker, or `index.html` loads this directory.

Run `node scripts/extract-release-62.mjs /tmp/kagenexus-release-62` to verify and reconstruct the exact pre-rebuild source snapshot.
